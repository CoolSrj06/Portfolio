After making any changes to the webisite and pushing it to GitHub.
Finally, you can: npm run deploy
This will:
    Build your project (npm run build).
    Deploy the build folder to the gh-pages branch of your repository.